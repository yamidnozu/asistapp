# ============================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO (FLUTTER)
# ============================================
# Copia este archivo a .env en la raíz del proyecto Flutter
# y edítalo según tu entorno de desarrollo.
#
# PRIORIDAD DE CONFIGURACIÓN:
# 1. --dart-define=API_BASE_URL=... (tiempo de compilación, mayor prioridad)
# 2. Archivo .env (este archivo)
# 3. Valores por defecto automáticos (SOLO en modo DEBUG):
#    - Android Emulador: http://10.0.2.2:3000
#    - iOS Simulator: http://localhost:3000
#    - Desktop: http://localhost:3000
#
# IMPORTANTE: 
# - En modo DEBUG sin configuración, se usan valores por defecto de desarrollo.
# - En modo RELEASE (producción), API_BASE_URL es OBLIGATORIO.
#   Debe configurarse via --dart-define en el workflow de CI/CD.

# URL base de la API
# ==================
# Para desarrollo local con Docker: http://localhost:3000
# Para emulador Android con Docker: http://10.0.2.2:3000
# Para dispositivo físico: http://<TU_IP_LOCAL>:3000
# Para producción: https://<TU_DOMINIO_PRODUCCION>
API_BASE_URL=http://localhost:3000

# Entorno (development, staging, production)
ENVIRONMENT=development
