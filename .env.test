# Archivo de configuración para pruebas E2E
# Este archivo contiene las variables de entorno específicas para las pruebas

# Base de Datos (usando la configuración de desarrollo para pruebas)
DB_USER=arroz
DB_PASS=pollo
DB_NAME=asistapp
DB_PORT=5433

# Backend (usando localhost para pruebas)
BACKEND_PORT=3000
JWT_SECRET="asistapp_secret_key_2025_super_secreta"

# Frontend API URL (localhost para pruebas)
API_BASE_URL=http://localhost:3000

# DATABASE_URL para Prisma (host)
DATABASE_URL="postgresql://arroz:pollo@localhost:5433/asistapp?schema=public"