import '../models/event.dart';

class EventEngine {
  List<Event> generateEvents() {
    // Placeholder: generate random events
    return [Event(id: 'random_event', description: 'Something happened', options: [])];
  }
}